# Auto Commiter 設定ファイルサンプル

# 自動コミットの対象範囲となるパスを指定します (必須)
# このパス以下が `git add` の対象となります
watchPaths:
  - src/
  # - docs/
  # - tests/
  # - path/to/specific/file.js

# 自動コミットを実行する間隔（秒単位、必須）
# デフォルトは 300 秒 (5分)
commitIntervalSeconds: 300

# 使用する LLM の設定 (必須)
llm:
  # プロバイダーを指定します (初回は 'openai' のみサポート予定)
  provider: openai
  # 使用するモデル名を指定します (例: gpt-4o-mini, gpt-4 など)
  model: gpt-4o-mini # <-- モデル名を変更
  # 必要に応じて、プロバイダー固有の追加設定を記述できます
  # 例: OpenAI の temperature など
  # options:
  #   temperature: 0.7

# コミットメッセージに付与する接頭辞 (任意)
# デフォルトは "[Auto commit]" などを想定
# commitPrefix: "[Auto commit]"